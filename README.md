# TapBridge

Remote launcher for desktop apps and websites from a React Native mobile app.

## Structure
- `mobile/` Expo React Native app
- `pc-client/` Node.js WebSocket service

## Quick Start
See the setup instructions in the response output or run:

```bash
cd pc-client
npm install
npm run dev
```

```bash
cd mobile
npm install
npm run start
```
